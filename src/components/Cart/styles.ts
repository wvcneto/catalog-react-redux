import styled from 'styled-components';

export const Container = styled.div`
  position: fixed;
  right: 0;
  text-align: center;
  margin: 4px;
  th {
    width: 64px;
  }

  background: #f1f1ff;
  color: #0e0e00;
  border-radius: 8px;
`;
